# needmoreusd@github:~$

```bash
git clone https://github.com/needmoreusd

cd poors

git branch
* poors

git status
# working tree clean

git checkout poors

git pull --rebase

find . -maxdepth 2

tree

ls -la

cat README.md

git log --oneline

git add .

git commit -m "poors"

git push origin poors

history | tail

exit
```
